# Absolute path to your career-ops checkout (used only by the Vite dev server
# to expose GET /api/career-ops/applications). Not bundled into the client.
CAREER_OPS_PATH=/Users/bazil/Documents/Programming/career-ops

# Absolute path to your interview prep markdown folder (used only by the Vite
# dev server as the default for GET /api/interview-prep/files). Override in
# the Live pack tab at any time.
INTERVIEW_PREP_PATH=/Users/bazil/Documents/interview-prep
